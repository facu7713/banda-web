<header class="absolute top-0 left-0 w-full z-20 flex justify-between items-center px-4 md:px-10 py-4 text-white -mt-40">

    <!-- LOGO -->
    <img src="{{ asset('imagenes/logo_pagina.png') }}" class="h-12 md:h-16 lg:h-20" style=" width: 600px; height: 400px;">

    <nav class="hidden lg:flex items-center gap-5 text-lg">

        {% set linkClass = "bg-black/60 rounded-full px-4 py-2 shadow-lg transition-all duration-300 hover:bg-black/80 hover:scale-105 hover:shadow-2xl text-white font-semibold hover:text-purple-400" %}

        <a href="{{ path('inicio') }}" class="{{ linkClass }}">Inicio</a>
        <a href="{{ path('banda') }}" class="{{ linkClass }}">La Banda</a>
        <a href="{{ path('shows') }}" class="{{ linkClass }}">Shows</a>
        <a href="{{ path('galeria') }}" class="{{ linkClass }}">Galería</a>
        <a href="{{ path('comentarios') }}" class="{{ linkClass }}">¿Tu Opinión?</a>

        <a href="{{ path('contacto') }}"
            class="bg-gradient-to-r from-purple-500 to-pink-500 px-5 py-2 rounded-full font-semibold hover:scale-105 transition">
            CONTRATAR
        </a>

    </nav>

    <!-- BOTÓN HAMBURGUESA (mobile + laptop) -->
    <button onclick="toggleMenu()" class="lg:hidden text-3xl">
        ☰
    </button>

</header>
<div id="mobileMenu"
    class="fixed top-0 left-0 w-full h-full bg-black bg-opacity-95 z-40 hidden flex flex-col items-center justify-center gap-8 text-2xl">

    <a href="{{ path('inicio') }}" onclick="toggleMenu()" class="hover:text-purple-400">Inicio</a>
    <a href="{{ path('banda') }}" onclick="toggleMenu()" class="hover:text-purple-400">La Banda</a>
    <a href="{{ path('shows') }}" onclick="toggleMenu()" class="hover:text-purple-400">Shows</a>
    <a href="{{ path('galeria') }}" onclick="toggleMenu()" class="hover:text-purple-400">Galería</a>
    <a href="{{ path('comentarios') }}" onclick="toggleMenu()" class="hover:text-purple-400">¿Tu Opinión?</a>

    <a href="{{ path('contacto') }}"
    onclick="toggleMenu()"
    class="bg-gradient-to-r from-purple-500 to-pink-500 px-6 py-3 rounded-full font-semibold">
        CONTRATAR
    </a>


    <!-- BOTÓN CERRAR -->
    <button onclick="toggleMenu()" class="absolute top-6 right-6 text-3xl">
        ✕
    </button>

</div>